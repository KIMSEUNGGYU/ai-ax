# fe-workflow v0.1 Architecture

```mermaid
graph TD
    subgraph fe-workflow["fe-workflow (v0.1)"]
        plugin["plugin.json<br/><i>name: fe-workflow</i>"]

        subgraph skills["Skills (auto-loaded)"]
            skill["fe-principles/SKILL.md<br/><i>코드 원칙 요약</i>"]
        end

        subgraph conventions["Conventions (SSOT)"]
            cp["code-principles.md<br/><i>코드 철학/원칙</i>"]
            fs["folder-structure.md<br/><i>폴더 구조/지역성</i>"]
            al["api-layer.md<br/><i>API/타입/React Query</i>"]
            eh["error-handling.md<br/><i>에러 핸들링</i>"]
        end
    end

    skill -->|"참조"| cp
    skill -->|"참조"| fs
    skill -->|"참조"| al
    skill -->|"참조"| eh

    claude["Claude Code"] -->|"자동 로드"| skill
    claude -.->|"필요시 Read"| conventions

    style plugin fill:#f0f0f0,stroke:#999
    style skill fill:#4a9eff,color:#fff
    style cp fill:#2ecc71,color:#fff
    style fs fill:#2ecc71,color:#fff
    style al fill:#2ecc71,color:#fff
    style eh fill:#f39c12,color:#fff
    style claude fill:#7c3aed,color:#fff
```

```mermaid
graph LR
    subgraph v0.1["v0.1 (현재)"]
        S1["Skill x1"]
        C1["Conventions x4"]
    end

    subgraph v0.2["v0.2 (예정)"]
        CMD["/review Command"]
        AGT["code-reviewer Agent"]
    end

    subgraph v0.3+["v0.3+"]
        CMD2["/architecture<br/>/api-integrate"]
        CMD3["/recap<br/>/organization"]
        HK["Hooks"]
    end

    v0.1 --> v0.2 --> v0.3+
    CMD -->|"참조"| C1
    AGT -->|"참조"| C1

    style S1 fill:#4a9eff,color:#fff
    style C1 fill:#2ecc71,color:#fff
    style CMD fill:#e74c3c,color:#fff
    style AGT fill:#9b59b6,color:#fff
    style CMD2 fill:#e74c3c,color:#fff
    style CMD3 fill:#e74c3c,color:#fff
    style HK fill:#f39c12,color:#fff
```

## Legend

| 색상 | 의미 |
|------|------|
| Blue | Skill (자동 로드) |
| Green | Convention (톤 정리 완료) |
| Orange | Convention (톤 정리 추후) / Hook |
| Red | Command |
| Purple | Agent / Claude Code |
